import { createContext } from "react";

// Create Authentication Context: to be able to use the id of the user all over the website
export const AuthContext = createContext() // the output is a component which will be used by other components