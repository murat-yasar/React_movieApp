import { createContext } from "react";

// Create MovieContext: to be able to use the id of movie all over the website
export const MoviesContext = createContext() // the output is a component which will be used by other components